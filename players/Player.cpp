#include "Player.h"
#include "../playfield/ships/Ships_factory.h"

void Player::init_playfield() {
    for(auto i: ship_set) {
        bool ship_accepted = false;
        while(!ship_accepted) {

        }

    }
}
